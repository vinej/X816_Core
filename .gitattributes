# Generated by tools/contract.py -- byte-compared by the STALE check, so git
# must never rewrite their line endings on checkout (that is exactly what
# happened on the first merge after generation: autocrlf stamped CRLF onto the
# working tree and the contract went "stale" with a content-empty diff).
boot/x816_contract.inc text eol=lf
